version: '3'
# CI build images and push to registry.
services:
    backend:
        image: ${CI_REGISTRY_API_WITH_PREFIX}_container_healtheck
        build:
            context: ../../
            dockerfile: backend/Dockerfile
        expose:
            - ${SERVICE_INTERNAL_PORT}